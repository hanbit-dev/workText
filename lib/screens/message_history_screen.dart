import 'package:flutter/material.dart';

class MessageHistoryScreen extends StatelessWidget {
  const MessageHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('메시지 내역 화면'));
  }
}
